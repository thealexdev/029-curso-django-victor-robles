# Cómo conectarse a MySQL con Python

Para poder hacer uso y conexión de las características del SGBD de MySQL, necesitamos instalar el siguiente paquete de Python con el comando proporcionado:

```bash
pip install mysql-connector-python
```
